# Optional SQLite path. If unset, the application uses ./kivi_memory.db.
# This file is a reference; set the variable in your shell because the app does
# not automatically load .env files.
KIVI_DB_PATH=kivi_memory.db
